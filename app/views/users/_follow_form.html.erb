<div class="container" style="width: 60%;">
  <div class="row">
    <div class="col-2">
    </div>
    <div class="col-10">
      <% unless current_user==(@user) %>
        <div id="follow_form"  style="width: 200px; padding-left: 45px">
        <% if current_user.following?(@user) %>
          <%= render 'unfollow' %>
        <% else %>
          <%= render 'follow' %>
        <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>